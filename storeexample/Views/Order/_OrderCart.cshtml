@model storeexample.Models.OrderViewModel

<h3>Order Cart</h3>

@foreach (var orderedProduct in Model.Order.OrderedProducts)
{
    <div>
        <span>@orderedProduct.Product.DisplayName - @orderedProduct.QuantityOrdered</span>
    </div>
}
